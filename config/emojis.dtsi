  ZMK_UNICODE_SINGLE(grin_squint, N1, F6, 0, 6);       // 😆
  ZMK_UNICODE_SINGLE(wink, N1, F6, 0, 9);              // 😉
  ZMK_UNICODE_SINGLE(smile_eyes, N1, F6, 0, A);        // 😊
  ZMK_UNICODE_SINGLE(heart_eyes, N1, F6, 0, D);        // 😍
  ZMK_UNICODE_SINGLE(thinking, N1, F9, 1, 4);          // 🤔
  ZMK_UNICODE_SINGLE(grin_sweat, N1, F6, 0, 5);        // 😅
  ZMK_UNICODE_SINGLE(upside_down, N1, F6, 4, 3);       // 🙃
  ZMK_UNICODE_SINGLE(smiling_imp, N1, F6, 0, 8);       // 😈
  ZMK_UNICODE_SINGLE(hand_over_mouth, N1, F9, 2, D);   // 🤭
  ZMK_UNICODE_SINGLE(party, N1, F9, 7, 3);             // 🥳
  ZMK_UNICODE_SINGLE(star_struck, N1, F9, 2, 9);       // 🤩
  ZMK_UNICODE_SINGLE(sob, N1, F6, 2, D);               // 😭
  ZMK_UNICODE_SINGLE(smirk, N1, F6, 0, F);             // 😏
  ZMK_UNICODE_SINGLE(rofl, N1, F9, 2, 3);              // 🤣
  ZMK_UNICODE_SINGLE(grin_smiling_eyes, N1, F6, 0, 4); // 😄
  ZMK_UNICODE_SINGLE(hug, N1, F9, 1, 7);               // 🤗
  ZMK_UNICODE_SINGLE(joy, N1, F6, 0, 2);               // 😂
  ZMK_UNICODE_SINGLE(kiss, N1, F6, 1, 8);              // 😘
  ZMK_UNICODE_SINGLE(smile_hearts, N1, F9, 7, 0);      // 🥰
  ZMK_UNICODE_SINGLE(cold, N1, F9, 7, 6);              // 🥶
  ZMK_UNICODE_SINGLE(zzz, N1, F4, A, 4);               // 💤
  ZMK_UNICODE_SINGLE(skull, N1, F4, 8, 0);            // 💀
  ZMK_UNICODE_SINGLE(crying, N1, F6, 2, 2);            // 😢
  ZMK_UNICODE_SINGLE(vomit, N1, F9, 2, E);             // 🤮
  ZMK_UNICODE_SINGLE(raised_eyebrow, N1, F9, 2, 8);    // 🤨
  ZMK_UNICODE_SINGLE(neutral, N1, F6, 1, 0);           // 😐
  ZMK_UNICODE_SINGLE(money, N1, F9, 1, 1);             // 🤑
  ZMK_UNICODE_SINGLE(exploding_head, N1, F9, 2, F);    // 🤯
  ZMK_UNICODE_SINGLE(sleep, N1, F6, 3, 4);             // 😴
  ZMK_UNICODE_SINGLE(zany, N1, F9, 2, A);              // 🤪
  ZMK_UNICODE_SINGLE(thumbs_up, N1, F4, 4, D);         // 👍
  ZMK_UNICODE_SINGLE(thumbs_down, N1, F4, 4, E);       // 👎
  ZMK_UNICODE_SINGLE(clap, N1, F4, 4, F);              // 👏
  ZMK_UNICODE_SINGLE(fist_left, N1, F9, 1, B);         // 🤛
  ZMK_UNICODE_SINGLE(fist_right, N1, F9, 1, C);        // 🤜
  ZMK_UNICODE_SINGLE(fingers_crossed, N1, F9, 1, E);   // 🤞
  ZMK_UNICODE_SINGLE(pray, N1, F6, 4, F);              // 🙏
  ZMK_UNICODE_SINGLE(red_heart, N2, 7, 6, 4);          // ❤️
  ZMK_UNICODE_SINGLE(black_heart, N1, F5, A, 4);       // 🖤
  ZMK_UNICODE_SINGLE(heart_hands, N1, F9, 0, D);       // 🫶
  ZMK_UNICODE_SINGLE(cupid_heart, N1, F4, 9, 6);       // 💖
  ZMK_UNICODE_SINGLE(raised_hands, N1, F6, 4, C);      // 🙌
  ZMK_UNICODE_SINGLE(wave, N1, F4, 4, B);              // 👋
  ZMK_UNICODE_SINGLE(pinched_fingers, N1, F9, 0, C);   // 🤌
  ZMK_UNICODE_SINGLE(sparkles, N2, 7, 2, 8);           // ✨
  ZMK_UNICODE_SINGLE(middle_finger, N1, F5, 9, 5);     // 🖕
  ZMK_UNICODE_SINGLE(dice, N1, F3, B, 2);              // 🎲
  ZMK_UNICODE_SINGLE(heart_present, N1, F4, 9, E);     // 💖
  ZMK_UNICODE_SINGLE(fire, N1, F5, 2, 5);              // 🔥
  ZMK_UNICODE_SINGLE(point, N1, F4, 4, 7);             // 👇
  ZMK_UNICODE_SINGLE(hand_left, N1, F4, 4, 8);         // 👈
  ZMK_UNICODE_PAIR(heart_on_fire, N2, 7, 6, 4, N1, F9, 4, 6); // ❤️‍🔥
  ZMK_UNICODE_SINGLE(palm, N1, F4, 5, 0);              // 👐
  ZMK_UNICODE_SINGLE(shaka, N1, F9, 1, 9);             // 🤙
  ZMK_UNICODE_SINGLE(handshake, N1, F9, 1, D);         // 🤝
  ZMK_UNICODE_SINGLE(writing, N2, 7, 0, D);            // ✍️
  ZMK_UNICODE_SINGLE(biceps, N1, F4, A, A);            // 💪
  ZMK_UNICODE_SINGLE(brain, N1, F9, E, 0);             // 🧠
  ZMK_UNICODE_SINGLE(no_hands, N1, F6, 4, 5);         // 🙅
  ZMK_UNICODE_SINGLE(facepalm, N1, F9, 2, 6);           // 🤦
